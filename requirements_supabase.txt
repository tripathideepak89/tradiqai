# Supabase Integration
supabase==2.15.2  # Pinned to prevent deep resolution
realtime==2.4.1  # Pinned for compatibility
# postgrest-py removed - supabase 2.15.2 already includes postgrest (new package name)
